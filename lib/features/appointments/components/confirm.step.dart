import 'package:flutter/material.dart';

class AfyaClinicConfirmation extends StatelessWidget {
  const AfyaClinicConfirmation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Confirmation');
  }
}